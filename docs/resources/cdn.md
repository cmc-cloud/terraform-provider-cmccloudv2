---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cmccloudv2_cdn Resource - terraform-provider-cmccloudv2"
subcategory: ""
description: |-
  
---

# cmccloudv2_cdn (Resource)



## Example Usage

```terraform
# create cdn for Origin Host
resource "cmccloudv2_cdn" "cdn1" {
	vod          = false
	name         = "vnexpress site"
	origin_type  = "host"
	domain_or_ip = "vnexpress.net"
	protocol     = "https"
	port         = 443
	origin_path  = "/content/"
}

# create cdn for S3 Host
resource "cmccloudv2_cdn" "cdn2" {
	vod            = false
	name           = "dantri site"
	origin_type    = "s3"
	s3_access_key  = "F7KL3TOAIEKPD4PQ6DRF"
	s3_secret_key  = "J4iwL4PyLPn1y79tg6fQsD2MwZruIEo39k2ckDG0"
	s3_bucket_name = "testbucket"
	s3_region      = "hn-1"
	s3_endpoint    = "s3.hn-1.cloud.cmctelecom.vn"
}

# create cdn for a VOD site 
resource "cmccloudv2_cdn" "cdn3" {
 vod          = true
 name         = "bongda"
 origin_type  = "host"
 domain_or_ip = "bongda.com.vn"
 protocol     = "https"
 port         = 443
}


# create cdn for a VOD site with S3
resource "cmccloudv2_cdn" "cdn4" {
	vod            = true
	name           = "bongda-s3"
	origin_type    = "s3"
	s3_access_key  = "F7KL3TOAIEKPD4PQ6DRF"
	s3_secret_key  = "J4iwL4PyLPn1y79tg6fQsD2MwZruIEo39k2ckDG0"
	s3_bucket_name = "testbucket"
	s3_region      = "hn-1"
	s3_endpoint    = "s3.hn-1.cloud.cmctelecom.vn"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain_or_ip` (String) The domain or IP of the CDN. Only valid for Host Origin type
- `name` (String) The name of the CDN
- `origin_type` (String) The origin type of the CDN: Host Origin or S3 Origin
- `port` (Number) The port of the CDN. Only valid for Host Origin type
- `protocol` (String) The protocol of the CDN, `http`, `https`. Only valid for Host Origin type
- `vod` (Boolean) If true, it will be the VOD site

### Optional

- `origin_path` (String) The origin path of the CDN, example `/content/`. Only valid for Host Origin type & vod is false
- `s3_access_key` (String) The access key of the S3 Origin. Only valid for S3 Origin type
- `s3_bucket_name` (String) The bucket name of the S3 Origin. Only valid for S3 Origin type
- `s3_endpoint` (String) The endpoint of the S3 Origin. Only valid for S3 Origin type
- `s3_region` (String) The region of the S3 Origin. Only valid for S3 Origin type
- `s3_secret_key` (String) The secret key of the S3 Origin. Only valid for S3 Origin type
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `cdn_url` (String) The CDN URL of the CDN
- `id` (String) The ID of this resource.
- `multi_cdn_url` (String) The multi CDN URL of the CDN
- `status` (String) The status of the CDN
- `updated_at` (String) The updated time of the CDN

<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String)
- `delete` (String)
